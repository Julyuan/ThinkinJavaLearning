内部类是定义在类内部的一种类
内部类拥有其外围类的所有访问权
.this可以获得外部类的引用
.new可以用来创建内部类

匿名内部类：方法的返回值是一个新定义的类

接口的内部也可以创建类

内部类可以实现多重继承的结构
例如我创建一个class，然后在它的内部类中实现各自的接口

闭包（closure）:一种可调用的对象，它记录了一些信息，这些信息来自于创建它的作用域。
内部类是面向对象的闭包。可以用闭包实现回调



